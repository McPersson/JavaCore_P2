1) Если значение переменной avg равно 100, то выведи на экран сообщение “Молодец”
        if (avg == 100) {
            System.out.println("Молодец");
        }

2) Если значение переменной mark меньше 60, то увеличить это значение на 10%
        if (mark < 60) {
            mark + mark / 10;
                }

3) Если значение переменной one больше значения переменной two, то вывести на экран значение one; в противном случае - вывести на экран значение переменной two
        if (one > two) {
        System.out.println(one);
        }
        else {
        System.out.println(two);
        }

4) Если значение переменной num больше нуля, то вывести на экран сообщение положительное.
        if (num > 0) {
        System.out.println("Положительное");
        }

5) Если значение переменной num меньше нуля, то вывести на экран сообщение Отрицательное
        if (num < 0) {
        System.out.println("Отрицательное");
        }

6) Если сумма переменных ugoll, ugol2 и ugol3 составляет 180, то вывести на экран сообщение “Это углы одного треугольника”; в противном случае
   вывести на экран сообщение “Это не углы одного треугольника”
        if (ugol1 + ugol2 + ugol3 == 180) {
                    System.out.println("Это углы одного треугольника");
                }
                else {
                    System.out.println("Это не углы одного треугольника");
                }